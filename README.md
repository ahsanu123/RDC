# 🧨 RDC - Rust Driver Collection

**RDC** (Rust Driver Collection) is a collection of driver with `embedded-hal` dependency and `#![no_std]` compitable, mostly it copied then modified from popular c/c++ library implementation.
